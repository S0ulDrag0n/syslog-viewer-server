version: '3'

services:
  db:
    image: linuxserver/mariadb:latest
    env_file:
      - .env
    ports:
      - 3306:3306
  viewer:
    image: s0uldrag0n/syslog-viewer-server
    depends_on:
      - db
    env_file:
      - .env
    ports:
      - 3000:3000
